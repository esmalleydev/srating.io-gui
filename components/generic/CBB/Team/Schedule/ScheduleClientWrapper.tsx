'use client';
import React from 'react';

const ScheduleClientWrapper = ({children}) => {

  return (
    <div style = {{'padding': '68px 5px 20px 5px'}}>
      {children}
    </div>
  );
}

export default ScheduleClientWrapper;
