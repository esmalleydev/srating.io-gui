
// this is NOT a client or server component
// it is both, do not mark it as one, these functions are shared... hence the name!

const getTagLabel = (): string => {
  return 'refresher.secret';
};

export { getTagLabel };
